<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.counter2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_beer"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round_beer"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SoberQuizActivity"></activity>
    </application>

    <!-- The "Beer" launcher icon was created by Freepik at www.flaticon.com.-->

</manifest>